# Proposed workflow changes

These files are inert copies of the GitHub Actions workflows with the build
metadata arguments needed by the version display. Apply their changes to the
matching files in `.github/workflows/` after this pull request is merged.
